<h1>Contacts</h1>

<%= link_to "Back to contacts", "/contacts" %> | <%= link_to "Edit", "/contacts/#{@contact.id}/edit" %> | <%= link_to "Delete", @contact, method: :delete, data: {confirm: "Are you sure you want to delete this contact?"} %>

<p><%= @contact.first_name %>, <%= @contact.last_name %>
<%= @contact.email %>, <%= @contact.notes %>
</p>
